# KORPUS v0.9.7

## v0.9.6 research/generalization hardening delta

PEC now separates software correctness from scientific authority: controller selection uses nested group-held-out validation; conditional risk admission is simultaneous and fail-closed; replay priority, information gain, sparse/dense signals and feature sufficiency are executable research reports; production authority requires bound production judgments. Architecture ratchets were reduced through module extraction rather than raised, and synthetic protocol evidence is explicitly non-promotional.

## v0.9.5 PEC act-hardening delta

PEC now enforces terminal controller abstention, non-duplicating original-query-first escalation, candidate-generation contextual retrieval from trusted metadata, complete replay/oracle/profile lineage binding, group-aware dataset leakage rejection, and fail-closed promotion evidence composition. Production promotion remains blocked until judged locked replay/ablation evidence demonstrates safety/quality non-inferiority plus measured efficiency superiority.

## v0.9.4 FULL SSOT integrity + 24-gate campaign delta

v0.9.4 makes FULL SSOT packaging an executable release invariant, makes current-truth verification portable across unpacked roots, rejects stale current aliases/release envelopes, and adds a predicate-level 24-gate campaign with causal blocker classification. It preserves the v0.9.2 private-network/canary production architecture and remains fail-closed for external evidence that has not actually been obtained.

# KORPUS v0.9.2
## v0.9.2 private-network + staged-promotion delta

v0.9.2 corrects a v0.9.1 evidence overclaim by implementing the previously claimed private Cloud SQL network plane in Terraform: dedicated VPC/subnet, Private Service Access, Cloud SQL public IPv4 disabled, and Direct VPC egress for API/worker/migration/verifier. It also changes production promotion from immediate full traffic to tagged candidate deployment, exact private candidate probing, bounded canary traffic, revision-specific minimum-sample/error-rate admission through Cloud Monitoring, full promotion only after admission, and revision-exact rollback on downstream failure. Terraform 1.15.8 and hashicorp/google 7.43.0 are pinned. Live production authorization remains fail-closed until external execution evidence exists.

# KORPUS v0.9.1

## v0.9.1 production infrastructure hardening delta

v0.9.1 hardens the production plane without expanding application authority: Terraform 1.15.8 and Google provider 7.42.0 are pinned; an offline HCL structural admission gate precedes hosted provider-schema validation; Cloud SQL auto-growth and Cloud Run capacity are explicitly bounded; migration history is immutable and future migrations are expand-only; provenance is verified before deployment; failed promotions restore revision-exact traffic; deep readiness, missing-telemetry, TLS-expiry and resource-pressure alerts are explicit; the external HTTPS proxy is bound to a MODERN TLS policy with TLS 1.2 minimum. Production authorization remains fail-closed until live external evidence is obtained.

# KORPUS v0.9.0

## v0.9.0 production infrastructure delta

v0.9.0 adds a production-deployment plane for Google Cloud: isolated GitHub OIDC/WIF trust planes for foundation, runtime and disaster-recovery workflows; Cloud Run services plus a non-HTTP Worker Pool; Cloud SQL PostgreSQL with HA/PITR and live RLS verification; native GCS object/audit storage with conditional-write semantics; Secret Manager integration; immutable image-digest deployment and Artifact Analysis gates; Cloud Monitoring SLO/burn-rate alerts; migration, smoke, backup and PITR-drill contracts. Production authorization remains fail-closed until live external evidence is obtained.

## v0.8.2 security/debt hardening delta

v0.8.2 hardens security-sensitive URL parsing and OIDC token context, refuses ambiguous credential-bearing/fragmented endpoints, enforces HTTPS for external model egress, preserves provider OIDC query metadata without allowing protocol-parameter shadowing, and formalizes a machine-readable AI risk profile. Existing module-complexity ceilings were not raised; affected modules were decomposed and the ratchet is re-bound to the reduced measurements. Formal production authorization remains external and fail-closed.

## v0.8.1 engineering-readiness campaign

v0.8.1 adds conflict-preserving evidence fusion, a bounded inference controller, a preregistered 94.7 engineering-readiness profile, current-truth generation/verification, and fail-closed ZIP safety before extraction. The readiness score is explicitly separated from formal production authorization; external trust predicates remain non-compensable. See `docs/assurance/READINESS_94_7_PROTOCOL.md`.

## v0.6.1 ACT-LRN-002 evidence-bound learning substrate

- immutable Course/CourseVersion/Module/Lesson/LessonBlock graph over canonical KORPUS evidence;
- exact document-version + evidence-span source bindings; no copied authoritative source text;
- fail-closed prerequisite, source-state, evidence-span and unbound-block validation;
- normalized 11-table learning persistence with migration `0016_learning_course_graph`;
- publication lifecycle `draft -> published -> invalidated/retired` with immutable review provenance;
- serving-time revalidation invalidates content when a bound source expires without a write event;
- PostgreSQL migration includes publication guards, historical immutability and source-change invalidation.

## v0.6.1 release doctrine

- release claims are source-bound to `SOURCE_MANIFEST.json` and executable evidence; historical reports are not promoted as current evidence;
- model or benchmark scores are never treated as a universal cognitive ceiling; capability claims identify the tested system, harness, budget and validity checks;
- broken-task, contamination, refusal, reward-hacking and evaluation-awareness hazards are explicit evaluation-validity concerns;
- software-security assurance follows the repository control map rooted in NIST SSDF / AI-specific SSDF guidance, while GenAI risk claims remain bounded by the NIST AI RMF GenAI profile;
- local self-evaluation cannot substitute for independent red-team, hosted-builder, vulnerability-scanner, production-like PostgreSQL or deployment attestations.

See `docs/architecture/PRODUCTION_HARDENING_REFERENCES_2026.md`, `config/assurance/standards-control-map.v1.json`, and `evals/EVALUATION_HARNESS_CONTRACT.json`.

## v0.6.0 bounded deterministic plasticity

- bounded adaptation is a pure content-addressed proposal: state + observation + policy + result;
- safety thresholds may tighten automatically but never relax through the adaptation controller;
- capacity/latency adaptation is bounded, cooldown-governed and requires explicit promotion outside the controller;
- deterministic replay gate compares exact test-outcome digests and semantic replay SHA-256 across hash seeds;
- metamorphic indirect-prompt-injection tests cover Unicode normalization, homoglyph and zero-width transformations;
- executable plasticity model-check exhaustively enumerates the finite release policy/state grid;
- coverage remains a ratcheted release gate; final v0.6.0 measurements are generated only after source freeze.


Evidence-bound knowledge, training and administrative platform for controlled Ukrainian document corpora.

KORPUS is not an autonomous operational decision-maker and does not treat an LLM as authority. Its trust kernel ingests immutable sources, derives identity and access server-side, filters inaccessible data before retrieval, selects temporally valid approved versions, emits exact extractive claims with content-bound citations, abstains when support is insufficient, and records decisions in a recoverable tamper-evident audit ledger.


## v0.4.0 production-hardening baseline inherited through v0.6.0

This release preserves the v0.3 formal assurance substrate and hardens it for a canonical production-candidate workflow: exact dependency-lock integrity, bounded exhaustive assurance model checking, fail-closed local production preflight, built-in high-confidence static security checks with destruction controls, indirect prompt-injection boundaries, release-ledger/state-machine verification, source-bound migration/inference evidence, and a fresh full backend/coverage campaign. The 16,500-vector deterministic adversarial corpus remains synthetic and contains no customer data or credentials. Production authorization remains independent of the weighted engineering readiness score and cannot be self-issued when external vulnerability scanning, independent red-team evidence, trusted hosted-builder attestation, exact deployment attestation, or production-like PostgreSQL evidence is absent.

## Implemented trust path

```text
verified OIDC identity
  -> application ABAC + PostgreSQL FORCE RLS
  -> temporal/current/review predicates
  -> bounded lexical + optional calibrated semantic candidates
  -> deterministic reranking and support gate
  -> extractive claim + offsets/hash citation
  -> database transaction + durable anchor outbox
  -> authenticated monotonic remote checkpoint
```

## Infrastructure topology

```text
127.0.0.1:3000
      |
non-root nginx/PWA -- internal edge -- API
                                      |-- internal backend --> PostgreSQL/pgvector
                                      |                    --> S3/MinIO object lock
                                      |                    --> OTel collector
                                      `-- dedicated egress --> OIDC / embeddings / audit anchor
```

Only the web proxy publishes a loopback host port. Backend networks are internal. Only the API joins the egress network.

## Current product state

- OIDC establishes identity; server-side content-addressed entitlements establish privileges.
- Need-to-know compartments and PostgreSQL RLS filter inaccessible text before retrieval.
- Browser access uses an opaque BFF session with PKCE, state, nonce and CSRF enforcement.
- Uploads are streamed to quarantine and processed through malware scan, parser isolation, bounded OCR and durable leased jobs.
- Source signatures, near-duplicate detection and extraction-quality warnings are mandatory review evidence in controlled mode.
- Metadata, content and approval stages require separate scoped reviewer credentials with expiry and revocation.
- Corpus policy controls classification, authority classes, OCR, indexing, citation, export, deletion, retention, legal hold and external embedding egress.
- Calibration profiles bind parameters to the dataset, evaluation protocol, system manifest and model configuration.
- Kubernetes/Kustomize provides a production reference topology with restricted Pod Security and default-deny networking.
- The complete v4 audit is preserved and all 99 findings are classified into locally closed, locally mitigated, external debt or open technical debt.

## Executable gates

```bash
make assurance
```

Current release evidence is source-bound under `reports/`. Counts in historical release
notes are not current-state claims. `reports/ASSURANCE_SNAPSHOT.json` and
`reports/RESEARCH_ASSURANCE_REPORT.json` are the machine evidence for the exact audited
source tree; production authorization remains a separate human gate.

## Local start

Requirements: Python 3.12+, Node 22+, Docker Compose for the integrated infrastructure profile.

```bash
cp .env.example .env
make infra-secrets
make infra-up
```

Web: `http://127.0.0.1:3000`

API through proxy: `http://127.0.0.1:3000/api`

Direct developer API: `make api-run`

Local development without Docker:

```bash
make api-install
make bootstrap
make api-test
make web-install
make web-build
```

## Repository map

```text
apps/api/                  FastAPI trust kernel and adapters
apps/web/                  dependency-free offline-capable PWA
packages/contracts/        versioned public schemas
infra/                     MinIO policies, telemetry and secrets contract
scripts/                   migration, assurance, backup, restore and packaging tools
evals/                     frozen adversarial fixtures
reports/                   content-hashed release evidence
docs/audit/                complete v4 audit and v5 99-finding closure
docs/architecture/         system and security topology
docs/assurance/            falsification strategy and audit ledger
agents/                    Codex / Claude Code worktree contracts
.github/                   primary GitHub CI, assurance, supply-chain and PR controls
.gitlab/                   retained legacy CI/parity evidence during migration
```

## Core killable invariants

1. Inaccessible text never enters candidate ranking, answers, citations, metrics or release identity.
2. Quarantined, rejected, future, expired, rescinded or superseded versions cannot answer outside their valid interval.
3. Every claim equals a cited substring and verifies by offsets, quote hash and source hash.
4. Retrieved text is data and cannot become a control instruction.
5. Missing or uncalibrated evidence produces an explicit abstention.
6. Metadata, versions, spans and audit events commit or roll back atomically.
7. Audit event chain, database head, outbox and remote anchor agree or readiness fails.
8. Candidate work is bounded before application reranking.
9. Fixed code, corpus release, calibration and query produce deterministic semantic output.
10. Backup recovery requires authenticated metadata, exact hashes/sizes, transactional restore, schema parity and RLS isolation.
11. Release evidence must match committed `HEAD`; mutable working-tree files cannot enter source packages implicitly.
12. Agent output cannot merge without independent review and protected GitHub gates; legacy GitLab parity cannot override them.

## Read first

- `FINAL_PACKAGE_CONTENTS.md`
- `VERIFICATION_REPORT.md`
- `docs/audit/closure/KORPUS_v5_CLOSURE_SUMMARY.md`
- `docs/architecture/SYSTEM.md`
- `docs/assurance/INFRASTRUCTURE_AUDIT_V4.md`
- `docs/assurance/FIRST_PRINCIPLES.md`
- `docs/engineering/FIRST_PRINCIPLES_2026.md`
- `docs/operations/GITHUB_REPOSITORY_POLICY.md`
- `GITHUB_IMPORT.md`
- `docs/assurance/TEST_STRATEGY.md`
- `docs/assurance/ASSURANCE_CASE.md`
- `docs/runbooks/BACKUP_RESTORE.md`
- `docs/runbooks/OPERATIONS.md`
- `AGENTS.md`

## Local Claude Code / Codex handoff

Start with `handoff/START_HERE_UA.md`. The chat is not authoritative; Git objects, manifests, machine reports and audit registers are.
