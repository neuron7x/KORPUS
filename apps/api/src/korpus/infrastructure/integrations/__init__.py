"""Infrastructure adapters for the governed capability gateway.

No adapter in this package owns authorization.  The application gateway resolves and
authorizes an exact server-owned capability before any adapter is called.
"""
