# Git import — KORPUS v0.9.7

Authoritative release identity: `v0.9.7`.
Canonical distribution artifact: `KORPUS_v0.9.7_PRODUCTION_ASSURANCE_HARDENED_FULL_SSOT_CANONICAL_2026-08-23.zip`.

Verify the archive SHA-256, distribution manifest, source manifest and package-build binding before
creating repository history. The package is intentionally gitless: source bytes are authoritative;
no unavailable commit, branch, signature or production attestation is invented. `LINEAGE/` is
provenance-only and must not be promoted into executable source.
