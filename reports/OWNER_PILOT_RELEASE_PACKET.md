# Пакет власника — приватний пілот KORPUS v0.9.7

**Кандидат:** `210762bce2651dff057b0ef9ff86f470584396d3`
**Дайджест джерела:** `45a3dbbf86b3e242e41b15b53f66cfacbce50adbcf747d9da03c8ee59293d6dd`
**Складено:** 04.09.2026
**Топологія:** `pilot-systemd-postgres-v1` — `korpus-pilot-api.service` на 127.0.0.1:8030, два робітники, CPUQuota 250 %, MemoryMax 3 ГБ; PostgreSQL 17 (pgvector) на 127.0.0.1:55440, ролі `korpus_app` / `korpus_authz` / `korpus_review`, межа RLS через брокера.

---

## 0. Вирок

**`TechnicalPilotReady(C*)` — НЕ ВИКОНАНО.** Кандидат не заморожений.

Це не відмова від релізу і не перелік побоювань. Це п'ять названих умов, три з яких
не може закрити жодна машина, бо вони вимагають ЛЮДЕЙ. Нижче — що виміряно, чим саме
воно виміряно, і що лишається невиміряним.

Чого цей пакет **не** робить: не оголошує систему придатною ухвалювати рішення,
не переносить жодного числа з пілотного корпусу на операційний і не підписує за
власника.

---

## 1. Що змінилось за ніч (усе — знайдене ЗАПУСКОМ, не аналізом)

| # | Вада | Як виявилась | Стан після |
|---|------|--------------|------------|
| 1 | **Під RLS повнотекстова умова не може бути умовою індексу.** `ts_match_vq`, `to_tsvector`, `ts_rank_cd` мають `proleakproof = f`, а безпекові умови стоять рівнем нижче, тож «securely promotable» ця умова не буває НІКОЛИ. GIN-індекс `ix_evidence_spans_search` існував і був недосяжний за побудовою; `to_tsvector` рахувався на кожному з 31 464 прольотів. | Профіль одного запиту: 98,6 % часу відповіді — у доборі. EXPLAIN під RLS 3,35 с проти 0,049 с без RLS. Вимкнення `nestloop` нічого не змінило — отже не план і не статистика. | Міграція 0023: генерований стовпець `search_vector`. **Добір 3,48 → 0,97 с; відповідь 3,6–4,3 → 1,3–1,9 с**, ті самі `span_id` на чотирьох питаннях. |
| 2 | **Навчання відновлення міряло втрату, яка не могла вийти ненульовою.** Контекст RLS клався через `set_config('korpus.*')` — протокол, якого схема 0020 не читає, тож `korpus_app` бачив НУЛЬ документів: `document_rows: 0`, `writes_after_backup: 0`, `lost_documents: 0`. Друга, незалежна причина: ідентифікатори записів «після бекапу» стабільні (uuid5) і вставка `ON CONFLICT DO NOTHING`, тож на базі, де навчання вже було, ці записи лежали В БЕКАПІ. | Прогін навчання на пілоті; те саме мовчки і в CI роками. | Прив'язка тепер ОДНА на дерево (`scripts/rls_context.py`), фаза «до бекапу» прибирає старі записи. **Втрачено 5 із 5** — результат, який міг вийти іншим. |
| 3 | **Проба навантаження, відхилена на дверях, задовольняла всі чотири перевірки SLO.** Токени протухли за годину: 101 206 відповідей 401 за хвилину, p95 0,006 с, жодного 5xx, жодного тротлінгу. | Випадковий прогін після зміни коду. | Виробник ВІДМОВЛЯЄТЬСЯ писати звіт, у якому фази SLO не дали жодного 200. |
| 4 | **Клас МАСШТАБУ виводився з класу СЕРЕДОВИЩА.** Щойно пілот став засвідченим `PRODUCTION_LIKE`, звіт відновлення оголосив масштаб «production-like» на 259 документах і 19 МБ — на два порядки нижче власної підлоги. | Гейт складання доказу відмовив як `OVERSTATED_SCALE`. | Клас масштабу рахується з виміряного обсягу; підлога імпортується з того ж модуля, який судить. |
| 5 | **Клас середовища не засвідчувався там, де немає init-системи.** Щойно перелік обов'язкових юнітів перестав бути порожнім, самоперевірка вперше дійшла до `systemctl` і впала в CI із `FileNotFoundError`. | Перший же прогін CI після зміни. | Відсутність init-системи = `unavailable` = `LOCAL_DEV`. Негативний контроль СТВОРЮЄ свою умову порожнім PATH. |
| 6 | **Топологія була оголошена двічі й розходилась.** Профіль твердих предикатів казав «топологія цього релізу — SQLite на loopback», конверт уже казав PostgreSQL. Той самий клас: процедура відкату спиняла ЧУЖИЙ юніт; пілотна база була оголошена й НЕОГОЛОШЕНА водночас (реєстр знає її за контейнером, процес ходить петлею). | Прогін `topology_agreement` (новий) і `corpus-axes`. | Розбіжність стала СТАНОМ, у три боки; `surface_aliases` називає другу адресу. |
| 7 | **Хибне речення про межу авторизації.** У конверті стояло «ключ у власника, агент його не має». Файл ключа має права 600 і належить ТОМУ САМОМУ UNIX-користувачеві, від якого працює агент. 600 боронить від інших, не від власного. | Перевірка прав файла. | Записано як є. **Це лишається відкритим станом, не закритою вадою** — див. §4. |
| 8 | **Доказ гейта губив причину власної відмови.** Хвіст писався як `(stdout + stderr)[-8000:]` — тобто останні 8000 символів ТІЄЇ труби, що прийшла другою; pytest називав відмову у stdout, alembic топив її журналом міграцій. | Гейт `postgres_security` казав FAIL і не міг назвати чому. | Бюджет НА ТРУБУ. Перший же прогін із виправленням назвав дві причини за секунди. |

---

## 2. Що виміряно на цьому кандидаті

### 2.1 Заморожений лан — 18 із 18

`evidence-refresh · api-test · api-lint · mutation · eval · migration-gate · scale ·
operational-gate · assemble-assurance · snapshot · validate · gate-liveness ·
release-surface · lane-report · corpus-axes · production-postgres-security ·
production-exact-environment-image · production-hard-predicates`

Вирок виробника — `BRANCH_CONSOLIDATION_ACCEPTED`, проблем нуль, невідомого нуль.
Тестів 3896, мутантів 613 із 613 убито.

### 2.2 Профіль відповідей — 16 осей, жодна не нижче підлоги

Найслабша вісь — **парафраз 0,75** проти підлоги 0,70. Предмет 0,967 (підлога 0,956),
своя межа 0,95 (0,9), чужа межа 0,80 (0,75), довідка 0,9367 (0,92), простежуваність
цитат 0,9648 (0,9648 — рівно на підлозі, запас нульовий).

### 2.3 Навантаження на пілотній топології (клас `PRODUCTION_LIKE`, 4 РІЗНІ суб'єкти)

| Фаза | Паралелізм | Запитів | p50 | p95 | Статуси |
|------|-----------|---------|-----|-----|---------|
| холодний перший | 1 | 1 | — | — | 200 за **2,151 с** (стеля 5,0) |
| навантаження | 4 | 46 | 2,634 | 3,594 | усі 200 |
| **soak (за ним судять SLO)** | 4 | 117 | 2,584 | **3,975** (стеля 5,0) | усі 200, **жодної відмови** |
| сплеск | 12 | 1242 | 0,057 | 1,06 | 113×200, 44×429, 1085×503 |

**Чесна стеля пілоту — чотири одночасні відповіді.** На сплеску 12 система відмовляє
через глобальну ємність (`KORPUS_MAX_CONCURRENT_ANSWERS=4`), а не через частку суб'єкта.
Для 3–10 запрошених це означає: одночасних питань більше чотирьох — п'яте отримає 503.

### 2.4 Відновлення (пілотна топологія, обсяг фікстури)

RTO **10,631 с** (відновлення 10,426 + перевірка 0,056) · RPO 0,0 с · втрачено подій 0 ·
**втрачено документів 5 із 5 зроблених після бекапу** · відновлена копія 259 документів,
19,6 МБ · ланцюг аудиту дійсний, схема на 0023.

**Клас масштабу — `ci-fixture`, не продакшен.** 259 документів і 19,6 МБ на два порядки
нижчі за підлогу претензії (100 000 рядків або 1 ГБ). **Час 10,6 с не переноситься на
операційний корпус.**

### 2.5 Машинна частина TEVV — 5 класів, усі PASS

| Клас | Питання | Значення | Межа |
|------|---------|----------|------|
| supported_retrieval | чи відповідає там, де відповідь є | 0,9367 | ≥0,92 |
| abstention_out_of_domain | чи мовчить там, де відповіді немає | 0,20 | ≤0,25 |
| in_domain_answer_rate | чи не мовчить там, де мусить | 0,95 | ≥0,90 |
| subject_disambiguation | чи відповідь про того, кого спитали | 0,956 | ≥0,956 |
| citation_identity | чи цитата належить названому джерелу | 1,0 | =1,0 |

Виміряно 03.09.2026 на дереві `25e42e77`; після цього змінювався добір (п. 1 §1), тож
**перед пілотом ці п'ять класів треба перезняти на кандидаті** — не тому, що є підозра,
а тому, що предмет виміру змінився.

### 2.6 Постава розгортання проти ВЛАСНОГО реєстру умов керованого середовища

**Виконано 9 із 35. Не виконано 24. Поза роллю рантайму 2.**

Виконано: PostgreSQL, схема під міграціями, Secure-cookie, стійкий ключ аудиту,
метрики увімкнені, явні довірені хости, HTTPS для S3/OTLP, розділення карантину GCS.

Не виконано, серед іншого: OIDC і браузерна автентифікація, TLS `verify-full` до бази,
профіль повноважень і його дайджест, профіль довіри до джерел, реєстр рецензентів,
профіль урядування корпусу, **калібрований режим відповідей**, розділення рецензентів,
віддалений якір аудиту, автентифікація метрик, довговічний інжест.

**Пілот оголошує клас `local`, тож рантайм НЕ ВИМАГАЄ жодної з цих умов.** Число вище —
те, що розгортання виконує само по собі, без примусу. Це не вада коду; це стан, який
власник мусить бачити ЧИСЛОМ, перш ніж запросити людей.

Окремо: `answer_policy_mode` = `development`, `calibration_id` = `development-unvalidated`.
Валідованого профілю калібрування в дереві немає — ні пілот, ні оцінки його не мають,
тож вони узгоджені між собою, але жодне число не спирається на калібрування.

---

## 3. Фінітний перелік блокерів (5)

| Предикат | Чого бракує | Хто може закрити |
|----------|-------------|------------------|
| `live_vulnerability_scanners` | `evidence_manifest_bound` — маніфест доказів постачання, зв'язаний із дайджестом цього кандидата | **Машина**: зелений конвеєр CI на цьому коміті |
| `trusted_hosted_builder` | `builder_provenance_verified` — провенанс збірки з хостованого CI для цього коміта | **Машина**: те саме |
| `real_domain_corpus_tevv` | ≥200 запитів із розміткою ДВОМА предметними анотаторами, ≥40 сліпих холдаутів, Cohen's kappa ≥0,6, суддя, який не є жодним з анотаторів | **ЛЮДИ.** Два контексти LLM не є двома незалежними анотаторами. Я цього не підроблю. |
| `independent_tevv` | `assessor_structured` — названий незалежний оцінювач | **ЛЮДИ.** Реєстр підписантів порожній; вигадати запис означало б підробити повноваження. |
| `production_like_tevv_environment` | прогін TEVV на замороженій топології | **ЛЮДИ** (залежить від двох попередніх) |

Замінники структурної незалежності на цьому коміті: **SI-3, SI-4, SI-8 — SATISFIED**;
SI-6 (чистий клон) і SI-7 (прогін CI) — виробляються, чекають завершення; SI-5 —
`NOT_MEASURED`, бо каталог мутантів не називає релізу (вада виробника, ремонт названий);
**SI-1 і SI-2 — `NOT_MEASURED`: засвідчення незалежного верифікатора відсутнє.** Я не
можу бути власним верифікатором і не записую згоди, якої не було.

---

## 4. Що знаю і не можу закрити технічно

1. **Ключ підпису токенів доступний агентові.** Файл 600 належить тому ж UNIX-користувачеві,
   від якого працює і сервіс, і агент. У режимі `jwt` токен НЕСЕ повноваження у claim'ах,
   тож хто має ключ — має будь-які ролі, допуск і корпуси. Справжня межа з'явиться лише
   коли ключ переїде туди, куди агент не дістає, або коли пілот піде на OIDC.
2. **Пілотний корпус має 8 документів навчання відновлення** (`recovery-drill*`) поверх
   256 доказових. Вони впізнавані за заголовком і не входять у жоден вимір якості.
3. **Причина пошкодження бази 03.09.2026 не встановлена.** Інцидент стався на локальному
   спадковому SQLite-розгортанні під час проби навантаження, яку запускала я. Відновлення
   з бекапу 03:33 підтверджене (256 документів, 31 464 прольоти, RTO 4,3 с), форензика
   збережена, причинність НЕ стверджується. `PRODUCTION_GUARD` на цю базу лишається.
4. **Лан споживає `var/recovery-report.json`, якого жоден його крок не виробляє.** Сьогодні
   навчання запускається окремим сценарієм поза деревом. Це названий борг.

---

## 5. Якщо власник вирішить відкрити пілот попри §3

Це рішення власника, не інженерне. Що для нього готове:

**Зупиняти негайно:** обхід авторизації · розкриття через межу · пошкодження бази чи
корпусу · повторювані необроблені 5xx · обслуговується НЕ той кандидат · зламана
прив'язка цитат до доказу · критична відмова журналу аудиту · невідновна проблема схеми.

**Не зупиняти:** косметична вада інтерфейсу · рідкісний некритичний крайній випадок ·
запит на нову можливість.

**Відкат** (виміряно, не оголошено):
1. `systemctl --user stop korpus-pilot-api.service`
2. відновити останній дамп — **10,4 с** на цьому обсязі
3. переключити `KORPUS_DATABASE_URL` на відновлену базу
4. `make serving-freshness` + `scripts/verify_audit.py`
Вікно втрати дорівнює віку останнього бекапу: **все, записане після нього, зникає** —
виміряно, 5 записів із 5.

**Що сказати запрошеним:** система відповідає ЦИТАТАМИ з корпусу 256 документів і
мовчить, коли підстав немає. Вона не ухвалює рішень. Одночасних питань — не більше
чотирьох. Відповідь приходить за ~2–4 с.

---

## 6. Рішення, яких я не ухвалювала і не ухвалю

- не оголошувала пілот придатним;
- не підробляла анотаторів, оцінювача чи підписанта;
- не пом'якшувала жодного SLO, щоб гейт позеленів;
- не записувала згоди верифікатора, якої не було;
- не переносила числа з фікстури на операційний корпус.

Заморозка (`make release-freeze`) відмовиться, доки блокерів §3 п'ять. Це правильна
відмова, і вона є частиною доказу, а не перешкодою йому.
