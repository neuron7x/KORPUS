# KORPUS v0.9.4 — Exact Optimization Pass

Date: 2026-08-20
Mode: semantics-preserving / fail-closed / no feature expansion.

## Accepted optimizations

1. Retrieval scoring: reuse normalized/tokenized query and candidate representations; skip zero-weight score components; skip MMR redundancy work when diversity lambda makes it algebraically irrelevant; reuse lexical span-id membership during semantic fusion.
2. Evidence support: remove redundant normalize-tokenize-normalize work while preserving token identity and support decisions.
3. Retrieval materialization: stop Pydantic materialization after the requested number of valid candidates has been produced; domain currency re-check remains fail-closed.
4. Release identity: factor the four-field temporal validity rule into one pure domain predicate. `DocumentVersionRecord.is_valid_on` and projection-only release-id evaluation now call the same rule, eliminating construction of full Pydantic records solely to test four dates.
5. Candidate SQL preparation: sort authorized corpus identifiers once and reuse the deterministic order for placeholders and parameters.
6. Audit readiness: reuse one post-anchor database checkout for historical-anchor verification and recoverable-gap counting; remote anchor I/O remains outside database checkout.
7. Source provenance hashing: stream source files in 1 MiB chunks rather than loading complete files into memory, with a fail-closed size-change check.

## Equivalence evidence

- Deterministic randomized temporal-predicate equivalence: 500 cases, fast field predicate == domain-model `is_valid_on`.
- Focused regression suites covering retrieval, evidence, citations, authority, query planning/cache, audit, anchor semantics, source provenance, current-truth hardening and mathematical integrity: PASS; one pre-existing skip in the selected set.
- Full API suite was started but exceeded the execution window after roughly 10% completion; no failure was observed before timeout. Therefore a complete all-suite verdict is UNKNOWN, not PASS.

## Measured hot-path delta

Synthetic projection-only currency benchmark, 5,000 version rows x 10 passes, identical outputs:

- previous full-Pydantic probe: 0.202072 s
- shared pure-field predicate: 0.007628 s
- observed speedup: ~26.5x for this isolated hot path

This benchmark is a local microbenchmark, not a production latency claim.

## Release-state consequence

The source tree changed, so source-bound historical reports that were generated before this pass are stale by construction. This candidate must not be promoted merely by rewriting their provenance. Required evidence must be regenerated by its owning gates before canonical/production authorization.
