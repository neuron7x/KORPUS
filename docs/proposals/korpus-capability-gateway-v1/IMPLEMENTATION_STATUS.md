# Implementation status

State: IN_PROGRESS

No work package is accepted until its tests and integration checks pass on the implementation branch.

Current execution order follows `IMPLEMENTATION_CAMPAIGN.md` and `IMPLEMENTATION_GRAPH.yaml`.

Merge remains prohibited until explicit owner authorization.
