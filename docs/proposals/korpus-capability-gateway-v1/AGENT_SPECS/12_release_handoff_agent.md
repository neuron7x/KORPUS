# Agent 12 — Release Handoff
Assemble exact commit, contract digests, enabled profile, tests, negative controls, CI,
clean-room, migrations/config, P0, N+1, deployment and rollback. Final state is
`READY_FOR_OWNER_APPROVAL`; never self-authorize production.
