# Agent 05 — Adapter and MCP
Implement thin adapters only after core control flow exists. Start internal, then HTTP, then
MCP if enabled. Remote auth/schema/description is protocol/discovery data, not KORPUS
authority.
