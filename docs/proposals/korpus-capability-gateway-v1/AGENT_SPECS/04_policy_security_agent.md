# Agent 04 — Policy and Security
Prove policy precedes execution. Attack caller trust fields, model arguments, MCP metadata,
confused-deputy paths, egress widening, capability substitution and credential leakage.
Critical bypasses require failing tests before fixes.
