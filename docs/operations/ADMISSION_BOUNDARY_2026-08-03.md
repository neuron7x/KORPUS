# Підстави недопуску КОРПУСу до експлуатації

Станом на 3 серпня 2026 р., гілка `integration/canon-v6`, база `bbebc0f`.

Цей документ існує, щоб недопуск був **рішенням із переліченими підставами**, а не
наслідком того, що ніхто не запитав. Кожен пункт нижче або знято вимірюванням, або
лишається чинним із посиланням на місце в коді.

`production_authorized = false`. Нижче — чому.

> **Оновлення 05.08.2026 (друге).** §2.8 знято: перелік предикатів, які повідомляють
> гейти випуску, читається з коду, і для кожного існує випадок, що робить саме його
> хибним (`test_gate_negative_controls.py`, **35 перемикачів на 35 предикатів**:
> `OPERATIONAL_BREAKERS` 19 + `ASSURANCE_BREAKERS` 16, і тест валить збірку, якщо
> множини предикатів і перемикачів розійшлися бодай на один — 02.09.2026 тут стояло
> «36 випадків на 32 предикати»). Новий
> предикат без такого випадку валить збірку — вимога тепер стоїть на майбутнє, а не
> лише на чотири гейти, які атакували.
>
> Разом із цим цей документ перестав бути єдиним місцем, де живуть підстави:
> `config/operations/admission-grounds.json` називає кожну підставу, клас доказу, який
> її знімає, і власника цього доказу, а `production_authorized` **обчислюється** з
> реєстру замість того, щоб бути константою. Підставу класу
> `external_assessment`/`owner_decision`/`measurement` не можна зняти правкою в цьому
> дереві: потрібна атестація (документ, його хеш, хто підписав, коли). Чинними
> лишаються 2.5, 2.6, 2.7 і `superseded-never-current`.
>
> **Оновлення 05.08.2026.** Знято 2.0.1 (відповідь, спростована власним доказом) і
> межу «доведено лише на SQLite»: увесь набір тестів виконується проти мігрованої
> PostgreSQL під роллю найменших привілеїв, і цей перший прогін знайшов три дефекти,
> яких SQLite показати не міг (ревізія схеми, семантика якоря аудиту, конкурентний
> запис). Разом із ними закрито чотири знахідки стадії руйнування поза реєстром:
> сторонній наступник, відкликання допуску, знаменник метрики витоку, аудит без
> керівної версії, нижня межа чинності. Перелік із місцями в коді —
> `docs/audit/INVARIANT_CLOSURE_2026-08-05.md`. Чинними лишаються 2.5, 2.6, 2.7 і
> частина 2.8; `production_authorized` не змінюється.
>
> **Оновлення 02.09.2026.** Стан звірено з `config/operations/admission-grounds.json`,
> який і є джерелом: ВІДКРИТІ — **2.5, 2.6, 2.7 і 2.9**; **2.8 знято** (`cleared`).
> Тобто абзац вище називав чинною зняту підставу і мовчав про чинну: слова «2.9» у
> документі не було ЖОДНОГО разу, хоч він і є переліком підстав недопуску. 2.9 —
> «Цілі відновлення (RTO/RPO) не оголошені», і прилад, що її закриває,
> `make sqlite-recovery-drill`, сьогодні не запускається взагалі
> (`bootstrap_local.py:95 → ReleaseIdentityUnavailable`).
>
> **Оновлення 04.08.2026.** Знято підстави 2.0, 2.0.2, 2.0.3, 2.1, 2.2, 2.3 і 2.4 —
> кожну виконуваним доказом (тест, що падає на дефекті, плюс мутант у каталозі).
> Перелік із посиланнями на місця в коді — `docs/audit/INVARIANT_CLOSURE_2026-08-04.md`.
> Чинними лишаються 2.0.1, 2.5, 2.6, 2.7 і 2.8; `production_authorized` не змінюється,
> бо 2.5–2.8 інженерними засобами всередині дерева не знімаються.
> Розділи нижче збережено в редакції 03.08: вони описують стан, у якому підставу було
> знайдено, і читаються разом із реєстром закриття.

## 1. Що система робить і чого не робить

КОРПУС знаходить затверджене джерело, показує, де саме в ньому написане твердження,
і відмовляється відповідати, коли корпус не тримає відповіді. Утримання — це
правильний результат, а не збій.

Система **нічого не підписує і не наказує**. Її вихід — матеріал для рішення людини,
а не рішення.

> **ВИПРАВЛЕНО 02.09.2026.** Тут стояло посилання «(ADR-0004, ADR-0005)». Цього рішення
> не містить ЖОДЕН із восьми ADR: 0004 — «Enforce access before retrieval», 0005 —
> «Bounded database candidate retrieval». Твердження чинне, підпори не було — і стояло
> воно в §1, тобто в описі системи для зовнішнього оцінювача. Виконуваний доказ самої
> властивості — `production_authorized=false` плюс порожні реєстри підписантів: без
> внесеного ключа жодна атестація не перевіряється, тож жодна підстава не знімається.

## 2. Підстави, які блокують допуск

### 2.0. Система здатна видати цитату, якої немає в документі

`apps/api/src/korpus/infrastructure/extraction.py:388`

Розбивач тексту склеює хвіст одного фрагмента з початком наступного через пробіл.
Утворене речення виходить у відповідь як дослівна цитата з `quote_hash` і
`source_hash`. Відтворено наскрізь 3 серпня 2026 р.: `status=answered`,
`quote in source == False`, `support_score=1.0`.

Перевірки, що доказовий фрагмент є підрядком документа, немає ніде в дереві.

Це підстава першого порядку. Ціннісна функція КОРПУСу — показати, **де саме**
написане твердження; система, яка показує неіснуюче місце і засвідчує його хешем,
не виконує єдину обіцянку, заради якої створена.

### 2.0.1. Відповідь може суперечити фрагменту, який сама ж цитує

`apps/api/src/korpus/application/answer_query.py:262` — детектор суперечності
порівнює вибрані твердження, а не текст фрагмента цілком. Відтворено: відповідь
стверджує «обов'язкове», цитуючи фрагмент, де наступним реченням написано
«не обов'язкове»; статус — `answered`.

### 2.0.2. Скасувати наказ неможливо

`apps/api/src/korpus/domain/models.py:198` — поле `rescinded_at` читається при
перевірці чинності, має мутанта в каталозі й описане в протоколі імпорту, але
**жоден шлях коду його не записує**. Єдиний доступний спосіб зняти чинність —
перевести версію в `REJECTED`: без мандата рецензента, без розділення обов'язків,
незворотно (`application/ingestion.py:38, 311`).

Для системи, що працює з нормативними актами, відсутність скасування — не прогалина
у зручності, а неможливість відобразити звичайну дію органу, який видав документ.

### 2.0.3. Чинність документа вирішує часовий пояс хоста

`apps/api/src/korpus/domain/models.py:275` — типове `as_of` береться з
`date.today()`. Відтворено: `TZ=Etc/GMT+12` → `answered`, UTC → `insufficient_evidence`
для того самого запиту в ту саму мить. Часовий пояс не закріплений ні в `Dockerfile`,
ні в `docker-compose.yml`, ні в маніфестах Kubernetes.

### 2.1. Застосунковий шар не перевіряє межу розкриття

`eligible()` у `application/answer_query.py:117→134` фільтрує кандидатів за оцінкою
релевантності, покриттям, класом авторитету і станом рецензування — і **не** фільтрує
за `corpus_id` та тиром доступу. Ці обмеження накладає лише SQL-запит у
`infrastructure/repository.py:384`.

`Retriever` — це `Protocol`-порт. Будь-який адаптер, який поверне спан поза межами
дозволеного принципалові набору корпусів, отримає його процитованим у відповіді.
Застосунковий шар не має способу це помітити.

Для системи, що працює з матеріалом різних тирів доступу, це блокувальна підстава,
і вона не знімається тестами: чинні тести використовують справний адаптер.

### 2.2. Порушення посилання проявляється як аварія, а не як утримання

Перевірки, що цитовані фрагменти належать саме тому набору доказів, який супроводжує
відповідь, немає. Якщо кількість цитат розійдеться з кількістю тверджень,
`evidence_coverage` перевищить одиницю і `ValidationError` перетворить це на
відповідь 500 (`application/answer_query.py:211`).

Правильна поведінка для цієї системи — покриття 0.0 і позначення всіх тверджень як
непідтверджених, тобто утримання. Аварія 500 не є утриманням: вона не повідомляє
оператору, що саме не підтвердилося.

Побічний наслідок тієї самої формули: чисельник рахує цитати, знаменник —
твердження, тож на практиці метрика набуває лише значень 0.0 та 1.0 і не розрізняє
відповідь із одним непідтвердженим твердженням від відповіді без жодного підтвердження.

### 2.3. Межа чинності документа не визначена і не захищена

`domain/models.py:227` порівнює `as_of > effective_until`, тобто документ лишається
чинним у день, зазначений як останній. Це може бути правильно — «чинний до 31 грудня»
українською зазвичай означає «включно з 31 грудня». Проблема не в напрямку нерівності.

Проблема в тому, що **семантика цієї межі ніде не зафіксована**. У `docs/`,
`contracts/openapi.json` і тестах немає жодного твердження про те, чи `effective_until`
є останнім днем чинності, чи першим днем нечинності. Перевірено мутацією 3 серпня
2026 р.: заміна `>` на `>=` не спричинила падіння **жодного** з 181 тесту.

Поруч, у тому самому методі, дата скасування трактується протилежно
(`as_of < rescinded_at.date()` — у день скасування документ уже нечинний). Дві межі
одного методу мають різну відкритість, і жодна з них не пояснена.

Наслідок для експлуатації: питання «чи діяв цей наказ на дату Х» система відповідає,
а на межі відповідає способом, який ніхто не обирав свідомо і який можна змінити,
не зламавши жодного тесту.

Порядок зняття: назвати семантику в `docs/architecture/DATA_MODEL.md`, закріпити
двома тестами (день до межі, день межі) і додати мутанта до каталогу.

### 2.4. Обмеження частоти не прив'язане до суб'єкта

Єдиний чинний ліміт — `limit_req` у `apps/web/nginx.conf:16` (оголошення зони) і `:53`
(сама директива; 02.09.2026 тут значився рядок 47 — там `access_log off`), ключований за
`$binary_remote_addr`. Наслідки: підрозділ за спільним NAT ділить одне відро;
зміна адреси обходить ліміт; звернення напряму до `api:8000` його не проходить взагалі.
`AdmissionController` (`application/resilience.py:47`) обмежує паралелізм глобально,
тому один суб'єкт здатен вичерпати ємність для всіх.

### 2.5. Автентифікація не пройшла незалежної оцінки

OIDC, ротація ключів, entitlements і governance корпусів у системі **є** — на відміну
від донорської лінії, де їх не було зовсім. Але жоден із цих механізмів не проходив
зовнішньої безпекової оцінки. Внутрішні тести писав той самий процес, що й код.

### 2.6. Немає TEVV на справжньому корпусі

Усі евали виконуються на синтетичних фікстурах
(`eval.calibration_status = UNVALIDATED_TEST_FIXTURE` у `reports/EVAL_REPORT.json`;
файла `RELEASE_METADATA.json`, названого тут до 02.09.2026, у дереві немає — оцінювача
слали в неіснуючий шлях, і §2.6 при цьому ВІДКРИТА підстава).
Жодного вимірювання на реальному масиві документів із реальними запитами не проводилось.
Показник калібрування, отриманий на фікстурі, не переноситься на бойові дані.

### 2.7. Питання класифікації та прав не вирішені

Не визначено, який матеріал система має право приймати, зберігати і відтворювати,
і хто це визначає. Це не інженерна підстава, і зняти її інженерними засобами не можна.

### 2.8. Чотири гейти неможливо завалити

Стадія руйнування (`docs/audit/DESTRUCTION_STAGE_2026-08-03.md`) показала з
відтворенням, що: операційний гейт приймає доказові артефакти з чужого дерева
(PASS із `source_commit='0'*40`); агрегатор доказів вважає успішним JUnit-звіт із
нуля тестів; продакшн-оверлей Kubernetes ніколи не рендериться, тому патч, що вимикає
`readOnlyRootFilesystem` і ставить образ `:latest`, проходить усі три валідатори;
реєстр закриття аудиту не перевіряє існування файлів, на які посилається як на
«виконуваний доказ».

Окремо: метрика витоку живиться з двох рядків датасету з тридцяти, тому ін'єкція
справжнього витоку між корпусами дала `30/30`, `leakage_failures=0` і
`access_noninterference=true`.

Доки ці гейти не здатні почервоніти, їхнє «PASS» не є свідченням про систему.

## 3. Підстави, які було знято 3 серпня 2026 р.

- **«Гейти зелені».** Були не зелені, а невиконані. Перший реальний прогін CI
  завершився падінням за 19 секунд: одна джоба впала на відсутньому `pydantic`,
  решта тринадцяти отримали `skipped`. Окремо: `mypy` не міг стартувати через
  неповний lock, а коли запрацював — показав 42 порушення strict-режиму, яких раніше
  ніхто не бачив; `ruff` брав конфігурацію з домашньої директорії користувача.
  Деталі й виправлення — ADR-0008.
- **«Історія релізів збережена».** Лінія v2.0.0–v5.1.0 існувала в єдиному локальному
  примірнику. Вивантажена до GitLab 03.08.2026.
- **«Дві системи еквівалентні».** Не еквівалентні: зі 27 доменних інваріантів
  донорської лінії 1 присутній, 15 послаблені, 11 відсутні
  (`docs/audit/INVARIANT_DIFF_2026-08-03.md`).

## 4. Що НЕ є підставою для недопуску

- Відсутність генератора природної мови. Провайдер за замовчуванням — заглушка,
  і будь-який інший провайдер зупиняє старт. Це навмисне: система показує знайдене
  місце в джерелі, а не переказує його.
- Транзакційний запис аудиту, що валить відповідь при збої
  (`application/answer_query.py:296`). Донорська лінія деградувала м'якше — рахувала
  збої і продовжувала. Тут суворіше правило лишено свідомо: відповідь без сліду в
  аудиті гірша за відсутність відповіді.

## 5. Порядок зняття

Підстава вважається знятою, коли існує **виконуваний** доказ, який можна зробити
червоним: тест, що падає на дефекті, або гейт CI, який блокує злиття. Документ,
звіт чи твердження в чаті доказом не є (ADR-0008).

Підстави 2.7 і 2.5 інженерними засобами не знімаються: перша потребує рішення
власника матеріалу, друга — сторонньої організації.
