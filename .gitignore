.env
.venv/
.venv
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.cache/
.ruff_cache/
.coverage
htmlcov/
node_modules/
.next/
dist/
build/
var/
*.db
*.sqlite
*.log
coverage.xml
junit.xml
.env.*.local
.DS_Store

# Ephemeral google-github-actions/auth credential files
gha-creds-*.json
# Terraform local state/cache/plan artifacts must never enter source control.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfbackend
crash.log
crash.*.log


# Образи й SBOM контейнерів — артефакти джоби, не вміст репозиторію.
# 05.09.2026 `git add -A` після локального відтворення постачальницьких доказів
# заніс api-image.tar (462 МБ) і web-image.tar (73 МБ) у коміт; обидва форжі
# відхилили push, і правильно зробили. Правило тут, щоб наступний `add -A` не повторив.
api-image.tar
web-image.tar
api-sbom.cdx.json
web-sbom.cdx.json
